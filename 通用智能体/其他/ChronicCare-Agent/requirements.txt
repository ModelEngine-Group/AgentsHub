# Reproducible Python 3.12 runtime, API, dashboard and validation dependencies.
pandas==2.3.3
PyYAML==6.0.3
networkx==3.6.1
pyvis==0.3.2
plotly==5.24.1
streamlit==1.58.0
fastapi==0.124.4
uvicorn==0.38.0
pydantic==2.12.5
Pillow==12.1.1
requests==2.32.5
httpx==0.28.1
sqlglot==30.12.0
mcp==1.26.0

# NPU execution is provided by the external Ascend/DataMate runtime.
# Install its matched CANN, torch, torch_npu and transformers builds there;
# generic PyPI wheels are intentionally not forced into this CPU-safe image.

# Test, coverage and static-quality dependencies.
pytest==8.4.2
pytest-cov==7.0.0
ruff==0.14.14
