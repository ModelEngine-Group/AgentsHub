# Nexent Skill 说明

当前三个智能体的业务能力由 MCP 工具提供，归档包没有必须额外上传的自定义 Skill，因此本目录暂时只保留说明文件。

后续可以自行编写 Skill，但应当满足一个清晰的复用场景，例如固定的文件处理流程、可复用的提示词编排或跨智能体的结果整理能力。新增 Skill 时，应将完整 Skill 包、依赖、绑定方式和一个可验证的使用示例一起放入本目录，并在对应的 Nexent 智能体配置中实际绑定；不建议为了填满目录而添加与现有 MCP 重复的空壳 Skill。
