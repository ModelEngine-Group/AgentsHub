# ============================================================
# 本地开发依赖 (development) — 测试 + 代码检查
# 在通用依赖之上叠加; 安装: pip install -r requirements-dev.txt
# ============================================================
-r requirements.txt

pytest>=8.0,<9.0
ruff>=0.4
# httpx 已包含在 requirements.txt 中 (API 测试客户端)
